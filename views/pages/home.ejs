<%- include('../partials/header') %>

<main>
  <div class="hero fade-in-down fade-delay-1">
    <img src="/images/bookBanner.jpg" alt="Education Banner" class="hero-img">
    <div class="hero-text">
      <h1>Welcome to the <strong>Lumina Academy</strong> Portal</h1>
      <h3>The gateway to your future</h3>
      <p>Connecting people, sharing knowledge, and empowering learning opportunities.</p>
    </div>
  </div>

  <section class="home-events fade-in-up fade-delay-2">
    <h2>Upcoming Events</h2>

    <div id="events">
      <% for (let i = 0; i < Math.min(3, events.length); i++) { %>
        <% const date = displayDate(events[i].startDate, events[i].endDate, events[i].startTime, events[i].endTime); %>
        
        <a href="/events">
          <div class="event fade-in fade-delay-<%= (i % 4) + 1 %>" id="event-<%= events[i].id %>" style="width: 28vw">
            <img src="/images/<%= events[i].image %>" alt="Background">
            <h3><%= events[i].title %></h3>
            <div class="info">
              <p><strong>Date:</strong> <%= date %></p>
              <p><strong>Location:</strong> <%= events[i].location %></p>
            </div>
          </div>
        </a>
      <% } %>
    </div>
  </section>
</main>

<%- include('../partials/footer') %>